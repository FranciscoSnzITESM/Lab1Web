#include <stdio.h>
int main()
{
    int x, y;
    printf("Enter the first integer: \n");
    scanf("%d", &x);
    printf("Enter the second integer: \n");
    scanf("%d", &y);
    printf("Suma = %d\n",x + y);
    return 0;
}